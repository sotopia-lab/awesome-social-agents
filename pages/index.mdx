import Link from 'next/link'
import { Lumiflex } from 'uvcanvas'
import { Button } from "../components/ui/button"
import { MoveRight } from "lucide-react"
import Charts from "./charts"

export default function Home() {
  return (
    <div className="home-content">
      <div className="relative inset-0 flex items-left justify-left h-full text-4xl font-bold py-3 px-3 pb-20">
        <div className="flex items-center justify-center h-full text-4xl font-bold">
          {/* <Separator className="mx-4" orientation="vertical" style={{ height: '200px', width: '4px'}}/> */}
          <div className="flex flex-col items-left">
            <div className="flex flex-row font-thin mb-2">
              Welcome to check out our collection of
            </div>
            <div className="font-bold mb-4 text-6xl">awesome social agents.</div>
            <div className="flex flex-row">
            <Link href="/paper_table"><Button className="group">  <div>Get Started</div>  <MoveRight className="translate-x-1 group-hover:translate-x-2"/></Button></Link>
            </div>
          </div>
        </div>
      </div>
      <Charts />
      </div>
  )
}

<Home />
